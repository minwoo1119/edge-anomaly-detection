# PyTorch and torchvision must be installed separately for the host CUDA/CPU platform.
# These versions reproduce the committed Anomalib notebooks.
anomalib==2.6.2
numpy==2.1.3
numba==0.61.2
onnx
onnxruntime
onnxscript
